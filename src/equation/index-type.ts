/**
 * Lambda expression definition for matcher
 *
 * - It takes 2 parameters and returns boolean
 * - Each lambda expression should be same signature
 * */
export type EquationMatcherLambda = (actual: unknown, conditionValue: unknown) => boolean;